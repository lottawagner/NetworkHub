# What's inside?
# a function to get_networkdata from a specific dbs, species, version


get_networkdata <- function(network_source,
                            species,
                            version,
                            cache = TRUE) {
  # do the right dispatches...

}
